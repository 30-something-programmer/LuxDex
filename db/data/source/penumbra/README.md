# Penumbra source data

`wild-encounters.txt` is the authoritative raw Penumbra wild-encounter export supplied to LuxDex. It is preserved byte-for-byte for future ingestion work. Pass 1 does not parse or publish it.

