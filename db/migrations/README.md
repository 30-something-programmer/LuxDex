# Migrations

Incremental application/database migrations will live here when schema evolution begins. Raw construction SQL under `db/schema/` remains authoritative for rebuilding the current schema from source.

