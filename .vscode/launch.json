{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "LuxDex API",
      "type": "debugpy",
      "request": "launch",
      "module": "uvicorn",
      "args": ["app.main:app", "--reload", "--port", "52031"],
      "cwd": "${workspaceFolder}",
      "console": "integratedTerminal"
    }
  ]
}

